{
  "name": "embed-width-adjuster",
  "version": "1.0.0",
  "main": "main.js",
  "scripts": {
    "build": "esbuild main.ts --bundle --outfile=main.js --external:obsidian --external:@codemirror/* --format=cjs --target=es2018"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "esbuild": "^0.27.2",
    "obsidian": "^1.4.0",
    "tslib": "^2.6.0",
    "typescript": "^5.0.0"
  }
}
